apiVersion: v1
kind: Service
metadata:
  name: udehiggjsguj
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "udehiggjsguj"
    port: 8080
  selector:
    app: udehiggjsguj
